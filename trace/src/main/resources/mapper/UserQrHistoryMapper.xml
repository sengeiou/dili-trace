<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dili.trace.dao.UserQrHistoryMapper">
    <resultMap id="BaseResultMap" type="com.dili.trace.domain.UserQrHistory">

    </resultMap>
    

    <select id="selectUserIdWithoutHistory" parameterType="com.dili.trace.dto.query.UserQrHistoryQueryDto" resultType="java.lang.Long">
      select user_id from `user` where user_id not in(
            select user_id from user_qr_history uq
            where <![CDATA[AND ( uq.created >= #{createdStart} OR uq.created <= #{createdEnd})]]>
            and is_valid=#{isValid}
      )
      <![CDATA[AND qr_status<>#{qrStatus})]]>
      <if test="userId != null">
        AND user_id = #{userId}
      </if>
  </select>
</mapper>